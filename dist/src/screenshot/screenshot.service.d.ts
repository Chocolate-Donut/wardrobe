export declare class ScreenshotService {
    generateOutfitImage(html: string, filename: string, canvasWidth: number, canvasHeight: number): Promise<string>;
    generateHTML(items: any[], width: number, height: number): string;
}
